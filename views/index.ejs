
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Book store</title>
</head>
<body>
    <div class = "container">
        <h1>A reader lives a thousand lives before he dies...</h1>
        <div>
            <button class="add-button"> <a href="/addbook">Add a book</a></button>
            <button class="reset-button"> Reset book list</button>
        </div>
        <div class="books">
            <% for (let i = 0; i< books.length; i++){%>
                <div class="book">
                    <img src="<%=books[i].img%>">
                    <p><%=books[i].title%></p>
                    <button class="delete-btn" id="<%=books[i].isbn%>">Delete</button>
                </div>
            <%}%>
        </div>
    </div>

    <script src="script.js"></script>
</body>
</html>